---
description: Auto-review pipeline — runs CEO+design+eng+DX reviews sequentially (gstack)
---
Use the `autoplan` skill (a gstack plan-review skill, auto-discovered from the skills directory). Invoke it via the Skill tool, then apply it to the context below.

Context / target: $ARGUMENTS
