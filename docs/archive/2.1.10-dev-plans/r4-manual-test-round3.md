# R4 手测清单——第三轮（两批修复回归 + R4 全面验收）

> 2026-09-08。基线 develop `76f1b41`（含 field-fixes-r4-1/r4-2 + #3 挂账清偿）。
> 前两轮反馈账目：一轮 #1/#4/#5（`27adac6`）、二轮 #1/#5（`31b50f1`）、#3（`0cc241d`）；
> #2（慢）/#4（远端目录浏览器）已定案并入 FIX-2 专项，不在本轮范围。
> 预计 30–45 分钟。反馈编号沿用 #N。

## 前置准备

- [ ] Windows dev Workbench 跑 develop 最新提交
- [ ] 一台可达远程机：SSH **密钥认证**（BatchMode 无交互，密码认证不可用），
      非交互 shell PATH 内有 `aisc`（WSL 现成方案：sshd :2222 + 公钥 + venv aisc，
      即 R1/R2/R3 自动化测试环境；真实 Linux 机亦可）
- [ ] 完整链路（D 组 svc 转发 + 容器会话）需远程机 Docker 可用；只验 UI 面可先不装
- [ ] 机器档案是全新面（S0 已清 F1 ssh_profiles）——正好从零走 A 组添加流程

## A. 机器管理页（R4b）

- [ ] **A1 添加**：设置 → 「远程机器（2.1.10）」组添加机器
      （名称/host/user/port/keyPath）→ 保存 → 重启 Workbench → 机器仍在
- [ ] **A2 编辑**：改端口/用户 → 保存 → 切换条下拉即时反映新值
- [ ] **A3 删除**：删除机器 → 下拉移除；若当前正驱动该机器，
      观察行为是否明确（不悬空、不半死状态）
- [ ] **A4 reset 不清档案**：设置页恢复默认 → remoteMachines 保留

## B. 驱动切换条 + recent 机器感知（R4b + 二轮 #1）

- [ ] **B1 渲染**：选择工作区页顶部切换条默认「本机」；下拉含全部机器
- [ ] **B2 切远程**：切到远程机 → 标题/徽章「远程：<名>」；
      工作区列表变为该机远端工作区（fs.list 探活）
- [ ] **B3 recent 徽章**【二轮#1】：recent 列表本机条目无徽章；
      POSIX 路径条目带「远程」徽章
- [ ] **B4 跨机点击**【二轮#1】：本机驱动态点一条远程 recent →
      自动切换驱动机器再探活，工作区可用
- [ ] **B5 切回本机**：列表恢复本地；无残留远程态/隧道（联动 E 组）

## C. 远程工作区创建 + 会话/文件面（一轮 #1 + R2/R3 回归）

- [ ] **C1 浏览禁用**【一轮#1】：远程模式下新建工作区「浏览」按钮禁用 +
      tooltip 指引手输远端绝对路径（如 /home/user/proj）
- [ ] **C2 远端探活**【二轮】：手输远端存在路径 → 校验通过（走 fs.list）；
      手输本机形态路径（C:\...）→ 按未找到处理，不炸不误判
- [ ] **C3 终端会话**（R2 回归）：开远程工作区终端 → PTY 流正常、
      命令有执行输出、拖拽窗口 resize 列数跟随
- [ ] **C4 远端文件树**（R3 回归）：浏览/懒展开/新建文件；
      远端 `touch` 一个文件 → 树自动刷新（watcher 全链）
- [ ] **C5 会话列表**（一轮 #4/#5 容错）：远程工作区历史 tab 正常渲染；
      无坏文件环境可跳过（容错路径已有单测）

## D. svc 网关端口转发（R4a）

- [ ] **D1 开服务**：远程 target 下服务页打开 web 服务 →
      浏览器 p<port>.localhost:<host> URL **可达**（隧道 ssh -L 同端口建好）
- [ ] **D2 复用**：关掉重开同服务 → 秒开（注册表 (target,port) 复用，不重建）
- [ ] **D3 切机拆除**：切另一台机器（或本机）→ 再开服务 → 指向**新机**的
      服务，旧机隧道不遮蔽同端口
- [ ] **D4（可选）**：`Get-Process ssh` 观察隧道进程随开关/target 切换的生灭

## E. 边缘与破坏性

- [ ] **E1 不可达机器**：切到错端口/关机的机器 → 报错清晰、不卡死；
      恢复后可重试
- [ ] **E2 直关 Workbench**：远程会话中途直接关 app → 重开无僵尸
      （远端 serve 进程应随 ssh 断开退出）
- [ ] **E3（可选，命中再验）**【#3】：网关端口 bind 失败时报错带
      WSL2/HNS 指引（wsl --shutdown + 管理员保留段配方）

## F. FIX-1 顺带验收

- [ ] **F1**：历史页 claude ✳ 暖金 / codex ◈ 冷蓝 图标辨识度达标

## 结果记录

| 项 | PASS/FAIL | 备注 |
| --- | --- | --- |
| A1 机器持久化 | PASS | |
| A3 删除移除下拉 | PASS | |
| A2 编辑生效 / A4 reset 不清档案 | 未测（可选） | |
| B2 切 nas 列远端 | PASS | 初次失败=远端未装 CLI（环境，装后通过） |
| B3 远程徽章 | PASS（修 #8 后） | 根因 normalizePath 翻斜杠 |
| B4 跨机自动切换 | PASS（修 #8 后） | 同上 |
| B5 切回本机 | PASS | |
| C1 浏览禁用+tooltip | PASS | |
| C2 路径校验 | PASS | 启动曾失败=修 #7（zsh 转义）后通过 |
| C3 终端（PTY/输出/resize） | PASS | |
| C4 文件树+watcher | PASS | 终端建文件秒现 |
| C5 远程会话列表 | PASS | |
| D1 服务经隧道打开 | PASS | nas 容器 http.server 经 ssh -L |
| D2 隧道复用秒开 | PASS | |
| D3 切机不遮蔽 | PASS（环境修复后） | HNS 圈占 47000-47999，wsl --shutdown 后恢复 |
| E1-E3 | 未测（可选） | |
| F1 图标辨识度 | PASS | ✳ claude / ◈ codex |
| #9 失败路径不进 recent | PASS（修 #9 后） | |

**结论：R4 手测三轮收官（2026-09-09）。修复批次 field-fixes-r4-1/2/3/4
全合入 develop。** 真机验证环境：nas（Debian 13.6/zsh/Python 3.13）+
wheel 安装 CLI + `~/.zshenv` PATH + docker save/load 运镜像。backlog 五项
见 devlog R4 手测收官条。
