# AISC Next

当前活动规划入口。代码基线：`d2bdcd9`。

阅读顺序：

1. `00-overview.md`：范围、目标、阶段治理；
2. `01-cross-stage-contracts.md`：跨语言和跨阶段不变量；
3. `02-dependency-map.md`：依赖和串行执行顺序；
4. `decisions.md`：已接受的架构决策；
5. 对应 `stage-*` 目录：阶段设计、实施和验收。

已完成规划只在 `docs/archive/completed/` 中保留。本目录中的规划完成后按整体阶段归档。
