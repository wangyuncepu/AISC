# v2.1.0-dev 测试清单

## 构建测试

- [ ] 镜像构建成功（`aisc build` 或 `docker build`）
- [ ] Claude CLI 版本验证（容器内 `claude --version`）
- [ ] Codex CLI 版本验证（容器内 `codex --version`）
- [ ] 无 npm 安装错误

## 启动测试

### 交互模式
- [ ] 作用域选择菜单正确显示（临时/项目）
- [ ] 启动方式菜单显示三个选项（bash/claude/codex）
- [ ] 选择 claude 正确启动 Claude Code
- [ ] 选择 codex 正确启动 Codex
- [ ] 选择 bash 进入命令行

### 配置目录
- [ ] 临时模式：使用 `/home/AISC/.claude` 和 `/home/AISC/.codex`
- [ ] 项目模式：使用 `/home/AISC/app/.claude` 和 `/home/AISC/app/.codex`
- [ ] `.claude` 目录正确复制（包含 skills 和 plugins）
- [ ] `.codex` 目录正确创建

### 环境变量
- [ ] `CLAUDE_CONFIG_DIR` 正确设置
- [ ] `CODEX_CONFIG_DIR` 正确设置
- [ ] `AISC_DIR` 正确设置
- [ ] 环境变量持久化到 `~/.bashrc`

## 功能测试

### Claude Code
- [ ] 可以正常启动并响应提示
- [ ] 可以通过 `cs` 命令切换后端
- [ ] skills 和 plugins 正常加载
- [ ] settings.json 环境变量注入正常

### Codex
- [ ] 可以正常启动
- [ ] 支持 OpenAI API 认证
- [ ] codex-wrapper 环境变量注入正常
- [ ] 独立配置目录功能正常

### 容器操作
- [ ] `aisc shell` 可以进入容器
- [ ] 容器内可以手动切换 `claude` / `codex`
- [ ] 支持 `docker run ... codex` 直接启动

## 兼容性测试

- [ ] Windows + Docker Desktop
- [ ] macOS + Docker Desktop
- [ ] Linux + Docker Engine
- [ ] WSL2 + Docker Desktop

## 文档验证

- [ ] README 版本号更新到 v2.1.0-dev
- [ ] Codex CLI 使用说明完整
- [ ] 快速开始部分包含双 CLI 说明
- [ ] CHANGELOG 记录完整

## 已知限制

1. Codex 需要 OpenAI API 认证（ChatGPT Plus/Pro/Enterprise 或 API Key）
2. 两个 CLI 使用不同的配置目录，不共享历史记录
3. 当前 WSL 环境无 Docker daemon，需在有 Docker 的环境测试
