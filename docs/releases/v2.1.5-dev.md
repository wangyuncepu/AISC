# AISC v2.1.5-dev

开发版本发布说明（按 `tests/packaging/test_release_notes.py` 契约维护；
正式 tag 发布时替换为对应版本号与最终说明）。

## Workbench（Phase 1–4 + GUI Fine-Tune 进行中）

- Phase 1（S1.1–S1.4）：CLI 发现/pin/capability 协商、PTY 会话数据面、结构化 CLI runner、错误脱敏与稳定错误码。
- Phase 2（S2.1–S2.4）：preflight/start/build 事件流、多 tab 会话、可观察性（freshness/stale）、history 持久化（schema v1，锁 + expected revision）。
- Phase 3（S3.1–S3.3）：request_seq/revision 防回退、provider 快照、tab 快捷键与焦点。
- Phase 4（S4.1–S4.2）：NSIS 安装器（依赖页、升级/卸载数据保留）、三平台 bundle CI、发布门文档。
- GUI Fine-Tune Step 0：canonical workspace 会话身份、SessionRegistry 所有权（Reserved/Closing/ack/TTL）、`shutdown_workbench` 退出协调器、安全原子替换、逐 TabRecord 布局恢复、前端测试设施（vitest）与 Workbench CI。

## CLI

- 结构化 `aisc.cli/v1` envelope；runtime/session/provider/doctor 子命令；session wrapper 容器内进程组管理。

## 已知限制

- Windows/macOS 正式发布需代码签名/公证（未配置）。
